import { styled } from '@mui/material';

export const PageWrap = styled('div')({
  width: '100%',
  height: '100%',
});

export const Background = styled('img')({
  width: '100%',
  height: '100%',
  zIndex: 1,
  overflow: 'hidden',
});
